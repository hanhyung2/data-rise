import * as S from './CustomerTable.styled.ts';

import Table from './Table.tsx';

const CustomerTable = () => {
  return (
    <S.TableWrapper>
      <Table />
    </S.TableWrapper>
  );
};

export default CustomerTable;
